/**
 * wizz gateway — WS-B builds the real service here against
 * docs/wizz-contracts.md and @wizz/contracts. This stub only pins the
 * workspace wiring so parallel builders never touch shared config files.
 */
import { WIZZ_GATEWAY_PORT_PUBLIC } from "@wizz/contracts";

export const PLACEHOLDER_PORT = WIZZ_GATEWAY_PORT_PUBLIC;
