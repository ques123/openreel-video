{
  "name": "@wizz/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared type + constant contracts between the wizz gateway and both apps/web build targets. Types and constants only — no runtime deps, no build step (consumers compile the TS source directly).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
